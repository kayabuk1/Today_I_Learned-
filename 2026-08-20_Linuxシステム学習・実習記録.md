# 2026-08-20 Linuxシステム学習・実習記録

## 1. 仮想・コンテナネットワークにおける論理境界線と結線検証

*   **`docker0` 仮想デバイス（`172.17.0.1`）の論理構造と存在理由**
    *   Dockerサービス（デーモン）の起動時に、ラズパイ（ホスト）内部のカーネル空間に自動生成される仮想スイッチハブ（ブリッジ） [user_query]。
    *   コンテナ個別のIPアドレスではなく、コンテナとホストOS間の通信を中継するためのゲートウェイ（ハブ）としての機能 [user_query]。
    *   コンテナの起動状態（生存期間）に関わらず、システム常駐のハブアドレスとして恒常的に存在し続ける仕様 [user_query]。
*   **`docker container port` コマンドが無言（無表示）となる物理的背景**
    *   ホストOSの物理ポートからコンテナの論理ポートに向けて、直通のポート転送土管（ポートマッピング）が未結線（未マウント）である状態 [20, user_query]。
    *   コンテナ起動時（`docker run`）に `-p` オプションによる明示的な結線命令を指定しない場合、マッピング情報が存在しないため表示対象データが皆無となる挙動 [20, user_query]。
    *   `-p 8080:80` 等によるポートフォワーディング指定時の、`80/tcp -> 0.0.0.0:8080` といった結線図（マッピング情報）の出力確認。

## 2. カーネルの物理資源認識と代替（ダミー）処理の動作機序

*   **`dmesg -l` オプションの厳格なパース（引数解析）仕様**
    *   深刻度（ログレベル）の指定（引数）を必須とするコマンド仕様。単独での `-l`（`--level`）実行時におけるパースエラー（引数不足）の発生。
    *   システムが例外処理ルートに入り、安全のためにコマンドの実行を強制終了（動作停止）させる機序。
    *   `dmesg -l err` や `dmesg -l err,warn` のように、カンマ区切りによる具体的なレベル指定を伴うパース解決手順。
*   **ラズパイ4における PCIe 電源警告（`vpcie3v3 not found`）の物理的要因**
    *   ラズパイ4の心臓部（BCM2711 SoC）に組み込まれた 1 レーンの PCIe バスによる、USB 3.0 コントローラ（VL805）の接続制御 [user_query]。
    *   基板スペースおよび省電力化のため、外部スロット用の独立物理電源回路（regulator）が非実装であるという物理構造の割り切り。
    *   カーネルの起動時（デバイスツリーのスキャンプロセス）における、定義に沿った電源供給チップの探索動作の空振り。
*   **代替電源（`dummy regulator`）による通信の維持**
    *   物理電源チップが非存在でも、別ルート（汎用回路）からすでに基板が通電されている状態。
    *   カーネルがメモリ上に仮想の仮電源（`dummy regulator`）を動的に割り当てる自動パッチ処理の実行。
    *   この代替処理の完了に伴う、USB 3.0 ポートの 100% 正常な通信状態の維持。

## 3. 極小コンテナ環境における特権プロセス隔離と手動通電（トラブルシューティング）

*   **コンテナマウント先における「絶対パス指定」の制約**
    *   `-v`（`--volume`）オプション使用時、コンテナ側の指定に `/` から始まる絶対パスを必須とする Docker デーモンの厳格なマウント規則 [20, user_query]。
    *   相対パス（スラッシュなしの `workspace`）指定時における、`mount path must be absolute` 判定に伴う起動処理の弾き（エラー） [20, user_query]。
*   **`ssh` および `sudo` コマンド不在の要因**
    *   「起動速度の最大化」および「ディスク容量削減」を優先した、プレーンなコンテナ用金型（RootFS）における徹底的なパッケージ削ぎ落とし仕様 [user_query]。
    *   クライアント機能（`ssh` コマンド）を含む `openssh-clients` パッケージの初期除外。
    *   コンテナ起動段階で最高権限者（`root`）としてログインしていることによる、管理者権限を一時借用する `sudo` コマンドの重複性排除（不要化）。
*   **コンテナ内での `systemctl` 実行不可のプロセス機序**
    *   コンテナ環境において大番頭プロセス（PID 1）として動くのは、システム全体のサービスを統括する `systemd` ではなく、昌敏さんが起動した `/bin/bash`（シェル）そのものであるという事実 [user_query]。
    *   サービス制御バス（DBus）の接続先である `systemd` が不在なため、`systemctl` を叩いても「話を聞く窓口が存在しない」となり動作拒否される仕様。
*   **SSH サーバーデーモン（`sshd`）の手動起動と root ログインの開通手順**
    *   `ssh-keygen -A` 実行による、各種暗号規格のホストキー（サーバーの身元を証明する看板）の自動一括生成プロセス [user_query]。
    *   一時ディレクトリ `/run/sshd` の手動確保、および `/usr/sbin/sshd &` 実行によるバックグラウンドプロセスの直接フォーク [user_query]。
    *   認証失敗（`Permission denied`）を引き起こす「2 つのセキュリティの関所」：
        1.  初期コンテナの `root` アカウントにおけるパスワード未設定（PAM 認証システムによる空・ロックパスワードでのネットワークログインの完全遮断） [user_query]。
        2.  SSH サーバー既定ルール（`/etc/ssh/sshd_config`）における、最高権限者の直接接続禁止指定（`PermitRootLogin prohibit-password`） [user_query]。
    *   `passwd` コマンドでのパスワード付与、`vi` エディタによる `/etc/ssh/sshd_config` の `PermitRootLogin yes` への書き換え、および `kill` を経た `sshd` のバックグラウンド再起動プロセスによる接続開通手順 [user_query]。
*   **`/usr/sbin` と `/usr/bin` の格納基準（身分制度）**
    *   システム利用者全員が実行可能な基本コマンド（`ls`, `cat`, `ssh` 等）を配置する `/usr/bin` フォルダ [user_query]。
    *   管理者（superuser）によるシステム保守・制御のみを想定した特権コマンドを配置する `/usr/sbin` フォルダ [user_query]。
    *   1023 番以下の特権ポート（22 番）の占有バインド、システムユーザー台帳（`/etc/shadow`）の参照、ログイン成功時の権限昇格（UID 切替）など、root 権限の動作機序が必須である `sshd` が `sbin` に配置される仕様。

## 4. cgroups によるCPU資源制限とタイムスライスの公平分配

*   **`stress-ng` 時間指定接尾辞のタイポエラー**
    *   実行時間の指定に数値単体や `sec` の記述を認めず、一文字の接尾辞 `s`（秒）、`m`（分）、`h`（時間）、`d`（日）、`y`（年）のみを評価する引数規則 [7, user_query]。
    *   コマンドの最後に時間を置くだけでは機能せず、時間のパースのために `--timeout`（または `-t`）のオプション看板を物理的に必要とする仕様 [7, user_query]。
*   **`--cpu-quota=50000` がもたらす物理的な cgroups 資源制御の天井**
    *   コントロールグループ（cgroups）機能により、カーネルがこのコンテナ全体の CPU 時間を「1 コア最大値の 50%（50,000μs / 100,000μs）」に物理ロックする枠制限 [20, user_query]。
    *   コンテナ内でどれだけプロセスを増殖（フォーク）させて無限ループを実行しても、全体の CPU 時間の合計が 50% で完全に頭打ちとなる物理現象 [20, user_query]。
*   **タイムスライスの公平分配による「等分」の動き（物理解読）**
    *   カーネルが 1 コアの演算時間をミリ秒単位に細切れ（タイムスライス）にし、対象プロセスに交互に割り当てる時間割の仕組み。
    *   コンテナ全体に許された「合計 50%」のパイを、動いている全負荷プロセス（`stress-ng` の子プロセス群）に公平に等分して配る動き。
    *   2 プロセス起動時は「24.9% ＋ 24.9% ≒ 約 50%」、3 プロセス時は「16.7% ＋ 16.7% ＋ 16.7% ≒ 約 50%」へと各プロセスの取り分が自動で等分減少し、合計値が天井（50%）に張り付く物理シミュレーション通りの動作確認 [user_query]。

## 5. ユーザー環境の記憶容量最大化とファイル転送の経路検証

*   **`~/.bashrc` における履歴の拡張と「後勝ち（オーバーライド）」の法則**
    *   既定値（1000 行）を大きく上回る `HISTSIZE=100000`（メモリ上保持）および `HISTFILESIZE=100000`（SDカード上の `.bash_history` 保存数）の設定拡張。
    *   `HISTTIMEFORMAT='%F %T '` 指定による、履歴ファイルへの実行日付・時刻（タイムスタンプ）の刻み込み仕様。
    *   Bash が設定ファイルを上から下へ 1 行ずつ読み込む評価仕様。ファイルの最下部に新規定義を追記（パッチ）することで、途中に書かれた `HISTSIZE=1000` などの古い既定値を安全に上書き（オーバーライド）する挙動。
*   **`vi` エディタにおける `i` と `o` の動作差と安全マージン**
    *   現在カーソルの直前から入力を開始する `i`（Insert） [user_query]。
    *   現在行の下に新しい空行を 1 行作成し、その先頭へカーソルを移して入力を開始する `o`（Open line） [user_query]。
    *   最終行ジャンプコマンド（`G`）の直後に `o` を叩くことで、既存の重要なシステム設定文字列の破壊を物理的に防ぎ、安全に追記行を確保するエディタ作法 [user_query]。
*   **Tera Term 「SSH SCP」転送が `No such file or directory` で空振りする罠の解剖**
    *   チルダ（`~`）の自動展開（パス翻訳）が、あくまで 「Bash（シェル）」という翻訳プログラムの中だけのルールであるという事実。クライアント側の入力ボックスではチルダが翻訳されず、そのままの名前のフォルダを探しに行ってエラーを吐く仕様。
    *   すでにラズパイとの間で SSH コネクション（暗号土管）が確立されている状態。その土管の内部を相乗りしてファイルを吸い上げる（SCP プロトコル）ため、`ssh://` や IP アドレス等のプロトコル指定を書いてしまうと「二重の結線」となって空振り（エラー）となる挙動。
    *   コンテナ内の `/tmp` や `/root` などの隔離空間（Namespace）に置かれたファイルは、ホストOSに繋がっている Tera Term からは物理的にスキャンできない境界線。ホストとマウント（共有）されている `/workspace`（ホスト側から見ると `~/sandbox`）の配下に置き、ホスト側の物理絶対パス（`/home/kayabuk1/...`）を指定して引っこ抜く必要性 [user_query]。

## 6. systemd におけるリンクの二重解決と起動ターゲットの論理構造

*   **`/sbin/init` から始まるシンボリックリンクの多重解決（デリファレンス）**
    *   `/sbin/init` ➔ `/lib/systemd/systemd` ➔ `/usr/lib/systemd/systemd` というポインタの連鎖構造。
    *   近代 Linux（UsrMerge）における、`/lib` ディレクトリ自体を `/usr/lib` へのシンボリックリンクに置き換えてプログラムを統合した物理構造。
    *   `ls -la /lib/systemd/systemd` を実行した際、OSが `/lib` を通過した段階で自動的にポインタの先へとパスをワープ解決（デリファレンス）させる挙動。
    *   その結果、`ls` コマンドが画面に描画するのは、ポインタ自体（`l`）ではなく、指し先にある「実体ファイル（通常ファイルを表す `-`、およびサイズ 133,584 バイト）」になるという動作仕様 [7, user_query]。
*   **`default.target` と `sysinit.target` の「最初」の論理的役割差**
    *   `default.target`：systemd がブートプロセスの一番最初に覗き込んで「今回の最終ゴールはどこか」をデリファレンス（参照）する、目的地の「ポインタ（シンボリックリンク）」。通常は `multi-user.target` や `graphical.target` を指し示す [105, user_query]。
    *   `sysinit.target`：目的地の決定後、実際に一番最初に実行（通過）される「物理セットアップターゲット（土台）」。仮想ファイルシステムマウントや SD カードの書き込み可能での再マウント、最小限のドライバロード等を実行する初期化関数の役割 [3, 4, 6, 7]。
*   **`/etc` と `/usr/lib` のオーバーライド（後勝ち）優先順位**
    *   systemd における、メーカー出荷時の既定値（`/usr/lib/systemd/system/`）よりも、管理者が手動変更したカスタム領域（`/etc/systemd/system/`）の設定を絶対的に最優先して読み込む鉄のルール。
    *   `sudo systemctl set-default multi-user.target` 実行時、`/etc/systemd/system/default.target` が `/usr/lib/systemd/system/multi-user.target` を指す強力な新規ポインタ（シンボリックリンク）として配置される仕組み。
    *   `cat` コマンドで両者を覗き込んだ際、OSの自動参照解決（デリファレンス）により、それぞれが指す異なる実体ファイルの中身が表示されること（DescriptionがCUI用/GUI用で異なる事実）の裏取り [105, user_query]。
