2026-08-09_Linuxシステム学習・実習記録
1. cat コマンドのパース仕様とC言語的動作イメージの確認
cat コマンド（concatenate：連結する）の本来の動作仕様 [14]
引数未指定時における、標準入力（ファイル記述子 0）からのデータ受信および一時バッファへのデータ吸い込み動作仕様 [14]。
各種オプションの略元および動作仕様の解剖 [14]
-n（number）：全行への行番号刻印。C言語におけるカウンタ変数（int line_counter = 1;）および改行コード（）検知時の printf("%6d ", line_counter++) ループ挿入処理の動作イメージ。
-b（number-nonblank）：空行以外の行番号刻印。if (buffer != ' ') によるカウンタ制御条件分岐の動作イメージ。
-E（show-ends）：行末への終端記号 $ の付与。画面上で不可視である改行コードの物理終端番地の可視化。
-T（show-tabs）：タブ文字の ^I への置換。ASCIIコードにおける9番目の制御文字（水平タブ）に対応する Ctrl + I（キャレット表記）の物理的結線の可視化 [14, 15]。
-v（show-nonprinting）：非印刷制御文字（isprint() == 0）の可視化。
-A（show-all）：上記 -v, -E, -T の一括起動（-vET） [15]。
cat -Anb のストリーム処理の裏取り [15]
入力文字 ] と Enter（）による ] バイト列データのパース処理 [15]。
-b による行番号の自動挿入および -E による改行コードの置換（$ 貼り付け）に伴う、標準出力上の 1 ]$ 描画の確認 [15]。
2. UUID構造と静的・動的IDの識別仕様
uuid コマンドおよび -d（decode）オプションの動作確認 [18, 22]
128ビット（16バイト）構造体のデコード動作確認 [18, 22]。
バージョン1（時間およびノードに基づく形式）UUIDにおける、タイムスタンプ、クロックシーケンス（14ビット乱数）、生成マシンの物理MACアドレス（88:a2:9e:23:f0:3d）のアンパック構造の確認 [18, 21, 33]。
14ビットクロックシーケンス（乱数スパイス）による衝突回避の動作機序 [33, 34]
時刻の逆行（NTP等による時刻補正）および極短時間内（100ナノ秒以下）の同時実行要求時に発生する重複不整合を防止するための衝突回避機能の仕様 [33, 34]。
静的識別子としての PARTUUID の特性 [26, 33]
OSインストール（フォーマット）時にストレージ（SDカード）の MBR/GPT 台帳に書き込まれる一意なID [26, 33]。
物理的な差し込み口やポート番号の変更時においても、ルートファイルシステム（/）の場所を確実に特定・マウントするための絶対アドレスマッピングとしての役割の確認 [26, 33]。
3. Raspberry Pi OS起動パラメータと物理的制御の解読
/boot/firmware/cmdline.txt に記述されたカーネル起動引数（argv）の物理的制御仕様の確認 [19, 24, 26]
console=serial0,115200：物理GPIOのシリアルポートから115200bpsでの起動ログパケット送信。ビデオカード非搭載の組み込み機器（ヘッドレス）におけるシリアルデバッグの役割 [24, 26]。
PARTUUID=...：マウント先ストレージ区画を一意に特定する物理配線。
quiet：起動時の大量のブートメッセージ（dmesg）の非表示制御 [18, 24, 27]。
splash / plymouth.ignore-serial-consoles：起動用ロゴマーク画面（スプラッシュ画面）による一時的な目隠しおよびシリアルピン優先出力制御 [18, 24, 27]。
cfg80211.ieee80211_regdom=JP：電波法（JP）適合レギュレーション（規制ドメイン）の強制ロードパッチ [18, 19]。
4. Dockerにおけるオブジェクト指向型制御とリソース解放の動作機序
docker container クラスが保持する各メソッド（サブコマンド）のオブジェクト指向的整理 [20]
attach, start, stop, run, rm, export, prune などのメソッド群の機能定義 [20]。
docker container port で何も表示されない背景 [29]
コンテナ起動時の引数 argv にポート転送用の -p オプションを指定しなかったことによる通信土管の未結線状態の確認 [29]。
prune メソッドの動作仕様 [29, 31]
語源（植物学における「剪定、枝下ろし」）の確認 [29, 31]。
残留した不要リソース（停止コンテナ、タグ切れイメージ）の一括ガーベジコレクション（解放）仕様 [29, 31]。
docker container export と画面ぶちまけ警告（cowardly refusing）の動作機序 [29, 30, 31]
コンテナ内の RootFS（最小限の動作に必要なフォルダ・ファイル構造全体）を1枚の .tar アーカイブに直列化（シリアライズ）してバックアップ出力する機能 [29, 34, 36]。
標準出力（1番の土管）へのバイナリデータの流出による端末フリーズを防止するための安全装置の作動確認 [29, 30, 31]。
-o オプションやリダイレクト（>）によるファイルへの出力退避仕様 [29, 30, 31]。
計算時間「タイムスライス」による並列マルチタスクの実現 [30, 31]
物理コアに対する極小の時間細切れ配給（コンテキストスイッチ）による同時稼働の擬似描画プロセス [30, 31]。
5. docker run 詳細パラメータの実機確認とマウント領域のUID透過性
実務運用を想定した重要起動パラメータの定義 [39, 40]
--rm：コンテナ停止時に、メモリおよびディスク上の一時フォルダなどのリソースを自動で完全解放（free）する、実験用自動お掃除オプション [39, 40]。
-e（環境変数）：コンテナプロセス空間への環境変数（MY_NAME等）の動的注入仕様 [39, 40]。
--hostname：コンテナ内でのホスト名（alma-lab等）の固定 [39, 40]。
-v（ボリュームマウント）：ホスト（~/sandbox）とコンテナ（/workspace）のディスク領域を繋ぐ接ぎ木マウントの動作仕様 [39, 41]。
起動後の実機動作確認結果 [41, 42]
[root@alam-lab /]# によるホスト名固定の確認 [41, 42]。
echo $MY_NAME による環境変数のデリファレンス（値の取り出し：Masatoshi）の確認 [41, 42]。
接ぎ木された /workspace 内の UID 1000 透過性の確認。コンテナ側で ls -la を実行した際、所有者名ではなく生の数値 1000 1000 で表示される動作機序（コンテナ側の名簿 /etc/passwd にホストの一般ユーザー kayabuk1 の登録がないため、生の数字をダンプする仕様） [51, 56]。
コンテナ内の root（UID 0）による、所有者 1000 のファイルへの書き込み許可（カーネルのアクセス制御ロジックにおける VIP バイパス仕様） [56, 57]。
--rm による停止時自動解放の確認（exit 後の docker ps -a にコンテナのなきがらが残留しない挙動） [40, 59, 61]。
6. コンテナの論理的隔離仕様と特権モードによるホスト乗っ取りの無限回廊
--mount と --volume（-v）の文法および設計思想の違い [43]
歴史的な簡易コロン区切り表記（--volume）と、key=value のカンマ区切りによる現代的な機能厳密区分表記（--mount）の比較 [43]。
FHS（ファイルシステム階層標準）に基づく実行ファイル格納場所の分類 [44]
/usr/bin（標準）, /usr/sbin（管理者用）, /usr/local/bin（自作コマンド）の役割分担 [44]。
pstree を用いた名前空間（Namespace）の隔離状況確認 [42, 44]
pstree -gahln 実行時における、コンテナ内プロセスの絶対的な目隠し効果の確認（PID 1 の bash と pstree のみがリストされる仕様） [42, 44]。
stress-ng の引数エラー発生要因の確認 [44, 45]
--cpu 0.5 指定時のエラー。C言語の atoi() 関数による整数型キャストを前提とした型設計上の制約 [44, 45]。
稼働率セーブには --cpu 1 --cpu-load 50 などのオプションを指定する仕様 [44, 45]。
特権モード（--privileged）によるホスト乗っ取り仕様の確認と動作テスト [48, 49, 50, 51, 55]
ソケット（docker.sock）経由の特権昇格（外から中への支配、--privileged 不要）に対し、特権モードはコンテナ内（中から外へ）の root がカーネルの「砂場の壁（目隠し）」を排除し、ホストOSの物理デバイスへの raw アクセス権を獲得する機能 [50, 51, 55]。
ホストOS（ラズパイ）の物理SDカード（/dev/mmcblk0p2）をコンテナ内の /mnt/host_root に直接マウント（Remount）し、/etc/shadow を覗き見・改ざんできることの確認 [48, 49, 52, 53]。
【無限回廊】：ラズパイ ⊃ コンテナ（プロセスフォーク）かつ、コンテナ ⊃ ラズパイ（物理SDカードマウント）という、メモリアドレスのポインタ（参照）書き換えが織りなす「鏡合わせ（ウロボロスの蛇）」の論理構造の把握 [58, 60]。
一般ユーザーへの --privileged 禁止制御：Samba の docker.sock を操作できる時点で、自動化やポリシー検問所（Admission Controller）を通さない限り、個別の禁止は不可能。防衛策としての Podman（デーモンレス）や Rootless Docker の導入 [55, 56]。
7. shopt コマンド（シェルオプション）の実機動作確認
shopt による状態一覧表示仕様の確認 [85]
-u（無効状態）および -s（有効状態）のオプション別リストアップ動作確認 [85]。
実機確認が完了した超重要オプション群 [89, 90, 91]
autocd：cd を打たずにパス名（ディレクトリ名）だけで自動移動する機能 [89, 90]。
cdspell：スペルミス（タイポ）を自動補正して正しいディレクトリへ移動する機能 [89, 90]。
globstar（**）：-R などの再帰オプションがなくても、すべての階層を自動で掘り下げるワイルドカード [89, 90, 91]。
extglob：ls !(file1.txt) のような、指定条件に一致しないものを論理否定（!）で除外して掴み取る拡張グロブ [89, 90, 91]。
8. fish シェルの履歴非共有仕様と bc コマンドのGPL法的背景
fish シェル導入時における履歴非共有の要因確認 [85]
Bash（~/.bash_history：プレーンテキスト）と fish（~/.local/share/fish/fish_history：YAML形式）におけるファイル形式および保存ファイルの差異の確認 [85]。
bc コマンド起動時の無保証メッセージの法的背景 [82, 85]
ABSOLUTELY NO WARRANTY（無保証）メッセージ。GNU GPL（一般公衆ライセンス）に基づき、「無料で自由に使えるが、何があっても自己責任（保証なし）」とするOSSの歴史的な仕様の確認 [82, 85]。
9. シグナル制御とクリーンアップ処理の物理限界
kill（PID指定）、killall（完全一致）、pkill（部分一致・属性フィルター）の機能比較 [86, 87]
対象プロセスを絞り込む際の条件指定における各コマンドの使い分け [86, 87]。
SIGTERM (15) と SIGKILL (9) の致命的な動作差異 [86]
SIGTERM（お行儀の良い終了要請）：プログラムにシグナルが届き、C言語のシグナルハンドラや Python の finally 句によるクリーンアップ（メモリやロックファイルの解放）を実行できる動作仕様 [86]。
SIGKILL（強制終了）：プログラムに届く前に「カーネルの手で直接メモリから消去（デリート）」されるため、プログラムカウンタが終了処理のメモリ番地に遷移できず、finally 句が一切実行されずに即死する動作仕様 [86]。
10. tmux 画面制御による実務上の防衛壁と疑似端末制限の確認
tmux（ターミナルマルチプレクサ）の基本機能定義 [88]
画面分割（Ctrl + b ➔ %（左右）や "（上下））、複数セッション管理、デタッチとアタッチの操作 [88, 90]。
実務における強力な防衛壁としての動作仕様の確認 [88, 90, 92]
作業中にネットワーク切断などの物理障害が起きても、サーバー側のプロセスは detached 状態で生きたまま保持され、再接続（tmux a）時に完全に復元（アタッチ）可能な動作仕様 [88, 90, 92]。
プロセス強制終了（SIGHUP）によるシステムの不整合・破損を防ぐ防弾仕様の確認 [88, 90, 92]。
rpi-connect における [exited] 急死の要因確認 [88]
疑似端末（TTY）の制御シグナルの結線が一部制限されていること。通常の SSH 接続時には、画面下に緑色のステータスバーがマウントされて正常起動する仕様の確認 [88]。
コピーペースト時の分割線再現の物理現象 [92]
画面の分割線（│ や ─）もグラフィックではなく、カーネルが「罫線文字（テキストデータ）」として画面メモリに描画しているため、テキストデータとして範囲選択してコピーされ再現される物理現象の確認 [92]。
